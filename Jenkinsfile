pipeline {
  agent any
  stages {
    stage('Inicio') {
      steps {
        sh 'env'
        echo 'Inicializando'
      }
    }

    stage('Instalación') {
      steps {
        sh 'gcc -v'
      }
    }

  }
}